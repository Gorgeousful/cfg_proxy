本项目针对headless服务器的代理设计。

在本地比如windows，将订阅链接导入clashverge UI，并找到订阅的位置，右键点击编辑文件，将文件内容复制。（UI的clashverge起到一个订阅链接->配置文件的作用）

将内容保存在本项目的conf/config.yaml中。

start.sh启动即可。

inspect.sh可以查看当前有多少个proxy进程在运行。

shutdown.sh关闭进程。
